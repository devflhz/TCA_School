@foreach (var item in Student.Addresses.Where(a => a.StudentId == Student.StudentId).Select(a => a))
{
    <p>@item.FormattedAddress</p>
    <br />
}
<button @onclick="Close" class="btn btn-secondary">Close</button>

@code {
    [CascadingParameter] BlazoredModalInstance ModalInstance { get; set; }
    [Parameter] public Student Student { get; set; }

    void Close()
    {
        ModalInstance.CloseAsync();
    }
}
